# @host = http://localhost:6000
@host = https://localhost/api/bearer
@contentType = application/json
@bearerToken = bearer_token

@channelId = 1


### Post message
POST {{host}}/message/{{channelId}}
Authorization: Bearer {{bearerToken}}

{
    "authorId": "Martin",
    "data": "Hello world!",
    "any": "junk"
}

### Get message
GET {{host}}/message/{{channelId}}
Authorization: Bearer {{bearerToken}}
